{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "outputDirectory":"/web/out",
  "buildCommand":"cd web && npm run build",
  "installCommand":"cd web && npm i && cd ../backend && npm i"

}